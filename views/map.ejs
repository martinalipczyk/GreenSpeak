<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pollution Map</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Enter Your Location</h1>
        <form id="locationForm" action="/pollutionMap" method="GET">
            <label for="zipCode">ZIP Code:</label>
            <input type="text" id="zipCode" name="zipCode" required placeholder="Enter your ZIP code">
            <button type="submit">Get Pollution Map</button>
        </form>
        <a href="/">Go Back</a>

        <div id="mapContainer" style="margin-top: 20px;">
            <% if (typeof wmsUrl !== 'undefined' && wmsUrl) { %>
                <h2>Pollution Map for <%= zipCode %></h2>
                <img src="<%= wmsUrl %>" style="width: 100%; height: auto;" alt="Pollution Map">
            <% } else { %>
                <p>Please enter a ZIP code to see the pollution map.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
