<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Learn About <%= zipcode %></title>
</head>
<body>
    <h1>Air Quality for ZIP Code: <%= zipcode %></h1>
    
    <% if (airData) { %>
        <p><strong>AQI:</strong> <%= airData.AQI %></p>
        <p><strong>Category:</strong> <%= airData.Category.Name %></p>
    <% } else { %>
        <p>No air quality data available for ZIP code: <%= zipcode %>.</p>
    <% } %>

    <% if (error) { %>
        <p><strong>Error:</strong> <%= error %></p>
    <% } %>
</body>
</html>