<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pollution Data for ZIP Code <%= dataList.zipCode %></title>
</head>
<body>
    <div class="container">
        <h1>Environmental Data for ZIP Code: <%= dataList.zipCode %></h1>

        <%console.log('Air Quality Data:', dataList.airQualityData.data)%>

<h2>Air Quality:</h2>
<% if (typeof dataList.airQualityData === 'string') { %>
  <p><%= dataList.airQualityData %></p>
<% } else { %>
  <p>Air Quality Index: <%= dataList.airQualityData.index.value %></p>
  <p>PM2.5: <%= dataList.airQualityData.pollutants.pm2_5 %> µg/m³</p>
  <p>PM10: <%= dataList.airQualityData.pollutants.pm10 %> µg/m³</p>
  <p>Ozone: <%= dataList.airQualityData.pollutants.ozone %> µg/m³</p>
  <p>So?: <%= dataList.airQualityData.health_recommendations.all %> </p>
<% } %>

<h2>Noise Data:</h2>
<% if (typeof dataList.noiseData === 'string') { %>
  <p><%= dataList.noiseData %></p>
<% } else { %>
  <p>Noise Index: <%= dataList.noiseData.noiseLevel.index.value %> dB</p>
  <p>So?: <%= dataList.noiseData.health_recommendations.all%></p>
<% } %>

<h2>Pollen Data:</h2>
<% if (typeof dataList.pollenData === 'string') { %>
  <p><%= dataList.pollenData %></p>
<% } else { %>
  <p>Pollen Index: <%= dataList.pollenData.index.value %> dB</p>
  <p>Main Pollen Pollutants: <%= dataList.pollenData.index.main_pollutants %> dB</p>

  <p>So?: <%= dataList.noiseData.health_recommendations.all%></p>
<% } %>

<h2>UV Index:</h2>
<% if (typeof dataList.uvData === 'string') { %>
  <p><%= dataList.uvData %></p>
<% } else { %>
  <p>UV Index: <%= dataList.uvData.index.value %></p>
  <p>So?: <%= dataList.uvData.health_recommendations.all%></p>
<% } %>

<h2>Water Quality:</h2>
<% if (typeof dataList.waterData === 'string') { %>
  <p><%= dataList.waterData %></p>
<% } else { %>
  <p>Water Contaminants: <%= dataList.waterData.index.value %></p>
  <p>So?: <%= dataList.waterData.health_recommendations.all%></p>
<% } %>

</body>
</html>
